model SystemCheckLog {
    id                 String                 @id @default(cuid())
    systemCheckService SystemCheckServiceEnum @map("system_service_name")
    createdAt          DateTime               @default(now()) @map("created_at")

    @@map("System_Check_Log")
}
