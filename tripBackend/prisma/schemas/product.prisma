enum productCategoryStatusEnum {
    active
    inactive

    @@map("Product_Category_Status_Enum")
}

enum mapAddressStatusEnum {
    active
    inactive

    @@map("Map_Address_Status_Enum")
}

enum locationStatusEnum {
    active
    inactive

    @@map("Location_Status_Enum")
}

enum productStatusEnum {
    active
    warning
    waiting
    inactive

    @@map("Product_Status_Enum")
}

enum productRateStatusEnum {
    active
    removed

    @@map("Product_Rate_Status_Enum")
}

enum productScheduleStatusEnum {
    active
    full
    canceled

    @@map("Product_Schedule_Status_Enum")
}

model productCategory {
    id          String            @id @default(cuid())
    name        String
    description String
    createAt    DateTime          @default(now()) @map("create_at")
    /// @DtoCreateHidden
    /// @DtoUpdateHidden
    updateAt    DateTime          @updatedAt @map("update_at")
    /// @DtoCreateHidden
    /// @DtoUpdateHidden
    deletedAt   DateTime?         @map("deleted_at")
    /// @DtoCreateHidden
    /// @DtoUpdateHidden
    status      productStatusEnum @default(active)

    product product[]

    @@map("Product_Category")
}

model providerMap {
    id          String    @id @default(cuid())
    name        String
    description String
    createAt    DateTime  @default(now()) @map("create_at")
    /// @DtoCreateHidden
    /// @DtoUpdateHidden
    updateAt    DateTime  @updatedAt @map("update_at")
    /// @DtoCreateHidden
    /// @DtoUpdateHidden
    deletedAt   DateTime? @map("deleted_at")

    mapAddress mapAddress[]

    @@map("Provider_Map")
}

model mapAddress {
    id               String               @id @default(cuid())
    urlMap           String               @map("url_map")
    providerMapId    String               @map("provider_map_id")
    /// @DtoRelationCanConnectOnCreate
    providerMap      providerMap          @relation(fields: [providerMapId], references: [id])
    createAt         DateTime             @default(now()) @map("create_at")
    /// @DtoCreateHidden
    /// @DtoUpdateHidden
    updateAt         DateTime             @updatedAt @map("update_at")
    /// @DtoCreateHidden
    /// @DtoUpdateHidden
    deletedAt        DateTime?            @map("deleted_at")
    /// @DtoCreateHidden
    /// @DtoUpdateHidden
    mapAddressStatus mapAddressStatusEnum @default(active) @map("map_address_status")
    ///@DtoRelationCanCreateOnCreate
    location         location?

    @@map("Map_Address")
}

model location {
    id           String             @id @default(cuid())
    systemName   String             @unique @map("system_name")
    displayName  String             @map("display_name")
    city         cityEnum
    mapAddressId String             @unique @map("map_address_id")
    /// @DtoRelationCanConnectOnCreate
    mapAddress   mapAddress         @relation(fields: [mapAddressId], references: [id])
    createAt     DateTime           @default(now()) @map("create_at")
    /// @DtoCreateHidden
    /// @DtoUpdateHidden
    updateAt     DateTime           @updatedAt @map("update_at")
    /// @DtoCreateHidden
    /// @DtoUpdateHidden
    deletedAt    DateTime?          @map("deleted_at")
    /// @DtoCreateHidden
    /// @DtoUpdateHidden
    status       locationStatusEnum @default(active)

    product product[]

    @@map("Location")
}

model product {
    id                String            @id @default(cuid())
    name              String
    supplierId        String            @map("supplier_id")
    /// @DtoRelationCanConnectOnCreate
    supplier          supplier          @relation(fields: [supplierId], references: [id])
    time              Int
    quantityAvailable Int               @map("quantity_available")
    age               Int
    quantityCompleted Int               @default(0) @map("quantity_completed")
    description       String
    quantityRate      Int               @default(0) @map("quantity_rate")
    avgRate           Int               @map("avg_rate")
    locationId        String            @map("location_id")
    location          location          @relation(fields: [locationId], references: [id])
    productCategoryId String            @map("product_category_id")
    /// @DtoRelationCanConnectOnCreate
    productCategory   productCategory   @relation(fields: [productCategoryId], references: [id])
    /// @DtoCreateHidden
    /// @DtoUpdateHidden
    createAt          DateTime          @default(now()) @map("create_at")
    /// @DtoCreateHidden
    /// @DtoUpdateHidden
    updateAt          DateTime          @updatedAt @map("update_at")
    /// @DtoCreateHidden
    /// @DtoUpdateHidden
    deletedAt         DateTime?         @map("deleted_at")
    /// @DtoCreateHidden
    /// @DtoUpdateHidden
    status            productStatusEnum @default(active)

    productImage    productImage[]
    productRate     productRate[]
    productSchedule productSchedule[]

    @@map("Product")
}

model productImage {
    id        String   @id @default(cuid())
    /// @DtoUpdateHidden
    productId String   @map("product_id")
    /// @DtoRelationCanConnectOnCreate
    product   product  @relation(fields: [productId], references: [id])
    url       String
    createdAt DateTime @default(now()) @map("create_at")

    @@map("Prodcut_Image")
}

model productRate {
    id        String                @id @default(cuid())
    /// @DtoUpdateHidden
    productId String                @map("product_id")
    /// @DtoRelationCanConnectOnCreate
    product   product               @relation(fields: [productId], references: [id])
    /// @DtoUpdateHidden
    userId    String                @map("user_id")
    /// @DtoRelationCanConnectOnCreate
    user      user                  @relation(fields: [userId], references: [id])
    star      Int
    /// @DtoCreateHidden
    /// @DtoUpdateHidden
    createAt  DateTime              @default(now()) @map("create_at")
    /// @DtoCreateHidden
    /// @DtoUpdateHidden
    updateAt  DateTime              @updatedAt @map("update_at")
    /// @DtoCreateHidden
    /// @DtoUpdateHidden
    deletedAt DateTime?             @map("deleted_at")
    /// @DtoCreateHidden
    /// @DtoUpdateHidden
    status    productRateStatusEnum @default(active)

    @@map("Product_Rate")
}

model productSchedule {
    id         String                    @id @default(cuid())
    /// @DtoUpdateHidden
    productId  String                    @map("product_id")
    /// @DtoRelationCanConnectOnCreate
    product    product                   @relation(fields: [productId], references: [id])
    startTime  DateTime                  @map("start_time")
    endTime    DateTime                  @map("end_time")
    price      Int
    booked     Int
    startOrder DateTime                  @map("start_order")
    endOrder   DateTime                  @map("end_order")
    /// @DtoCreateHidden
    /// @DtoUpdateHidden
    createAt   DateTime                  @default(now()) @map("create_at")
    /// @DtoCreateHidden
    /// @DtoUpdateHidden
    updateAt   DateTime                  @updatedAt @map("update_at")
    /// @DtoCreateHidden
    /// @DtoUpdateHidden
    deletedAt  DateTime?                 @map("deleted_at")
    /// @DtoCreateHidden
    /// @DtoUpdateHidden
    status     productScheduleStatusEnum @default(active)

    infoBill     infoBill[]
    Cacartrt     cart[]
    infoDiscount infoDiscount[]

    @@map("Product_Schedule")
}
